services:
  - type: web
    name: chopinow-backend
    env: node
    plan: free
    region: oregon
    buildCommand: "npm install && npm run build"
    startCommand: "node dist/main.js"
    envVars:
      - key: DATABASE_URL
        fromDatabase: chopinow-db
      - key: STRIPE_SECRET_KEY
      - key: CLERK_API_KEY
      - key: OPENAI_API_KEY
      - key: PINECONE_API_KEY
      - key: CLOUDINARY_URL
databases:
  - name: chopinow-db
    engine: postgresql
    version: "14"
    plan: free